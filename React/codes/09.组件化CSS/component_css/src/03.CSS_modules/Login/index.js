import React, { PureComponent } from "react";

import login from "./style.module.css";

export default class index extends PureComponent {
  render() {
    return <div id={login.title}>Login</div>;
  }
}
